/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import Root from './views/root';
import { AppRegistry } from 'react-native';
//var root = require('./views/root');

AppRegistry.registerComponent('BitEx', () => Root);
